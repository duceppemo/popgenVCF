% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/canonical_ancestry_three_backend.R
\name{new_ancestry_backend_evidence}
\alias{new_ancestry_backend_evidence}
\title{Create ancestry backend evidence for one backend's K sweep}
\usage{
new_ancestry_backend_evidence(
  backend,
  tool_version,
  command,
  k_values,
  replicates,
  seed,
  metric_name,
  stability_by_k,
  provenance = list()
)
}
\arguments{
\item{backend}{One of `admixture`, `faststructure`, or `snmf`.}

\item{tool_version}{Non-empty backend tool/package version string.}

\item{command}{Representative command or invocation template.}

\item{k_values}{Sorted, unique integer K values, each at least two.}

\item{replicates}{Positive integer replicate count per K.}

\item{seed}{Base deterministic seed.}

\item{metric_name}{Backend fit-metric name, or `NA` if unavailable.}

\item{stability_by_k}{Data frame with one row per `k_values` entry and
columns `k`, `metric_mean`, `global_stability`, `mean_alignment_score`.}

\item{provenance}{Named list of additional provenance fields.}
}
\value{
A validated `PopgenVCFAncestryBackendEvidence`.
}
\description{
Summarizes one backend's replicate stability and fit-metric behavior
across its declared K range. This does not retain raw Q matrices; those
remain in the run's own evidence directory. Stability and alignment
figures come from `consensus_ancestry()` run separately for each K.
}
