% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/execution-retry.R
\name{execute_analysis_plan_with_retries}
\alias{execute_analysis_plan_with_retries}
\title{Execute an analysis plan with bounded retries}
\usage{
execute_analysis_plan_with_retries(
  analysis,
  context,
  registry,
  plan,
  engine = new_execution_engine(fail_fast = FALSE),
  retry_policy = new_execution_retry_policy()
)
}
\arguments{
\item{analysis}{A `PopgenVCFAnalysis` object.}

\item{context}{Runtime context shared by module runners.}

\item{registry}{A `PopgenVCFRegistry` object.}

\item{plan}{A `PopgenVCFExecutionPlan` object.}

\item{engine}{Execution engine. Retry orchestration always evaluates a full
attempt with `fail_fast = FALSE`.}

\item{retry_policy}{A `PopgenVCFExecutionRetryPolicy` object.}
}
\value{
A regular execution result augmented with `attempt_execution` and
  retry metadata.
}
\description{
Each attempt uses the standard execution engine and validation contracts.
Successful modules are removed from subsequent plans, while failed and
blocked modules are eligible for another attempt only when every observed
failure is classified as retryable. All attempt rows are retained.
}
