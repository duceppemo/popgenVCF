% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/execution-scheduler.R
\name{execute_analysis_plan}
\alias{execute_analysis_plan}
\title{Execute an analysis plan with deterministic portable scheduling}
\usage{
execute_analysis_plan(
  analysis,
  context,
  registry,
  plan,
  engine = new_execution_engine(),
  checkpoint_path = NULL
)
}
\arguments{
\item{analysis}{A `PopgenVCFAnalysis` object.}

\item{context}{Runtime context shared by module runners.}

\item{registry}{A `PopgenVCFRegistry` object.}

\item{plan}{A plan returned by [plan_analysis_execution()].}

\item{engine}{A `PopgenVCFExecutionEngine` object.}

\item{checkpoint_path}{Optional `.rds` path. When supplied, an execution
checkpoint is written (overwriting any prior one) after every batch
completes, unconditionally -- not tied to any cancellation or failure --
so a later [run_pipeline_resume()] can pick up after the last completed
batch regardless of how the process ended (a crash, an out-of-memory
kill, `docker stop`, or anything else that a cooperative check between
modules cannot observe). A checkpoint-write failure (e.g. a full disk)
is logged as a warning, not fatal: it must never cost already-completed
analysis results.}
}
\value{
A deterministic execution result with scheduler provenance.
}
\description{
Parallel-safe modules in the same dependency wave may execute concurrently.
Worker completion timing is recorded, but validation and result merging always
follow the deterministic plan order.
}
